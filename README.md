# Agentic_Catalog_Engine
An agentic extraction pipeline and semantic search chatbot that transforms unstructured technical manuals into strictly validated JSON databases and vector embeddings for context-aware queries.
